let edad = 25;
let i = 0;


for (let index = 1; index < edad ; i++) {
    if (index = edad) {
        console.log(`el usuario tendria ${edad} años`)
    }else{
        console.log(`si no ingresa la edad correcta no se podra continuar`)
    }
}
console.log(`la edad del usuario es ${edad}`)

