// 13. Escriba una función que reciba un número entero y dicha función pueda determinar si el número enviado es positivo o negativo.

function entero() {
    let numero = parseInt(prompt("ingrse el numero entero"))//aqui pedimos el numero 
    if (numero > 0) { 
        alert(`el ${numero} es positivo`)
    }else if(numero = o){
        alert(`el ${numero} es no negativo`)
    }else {
        alert(`el ${numero} es negativo`)
    }
}

entero()
